cd "$(git rev-parse --show-toplevel)/online-proctoring/client" && npx lint-staged && npx vitest run && grep -q "chat-widget" index.html || { echo "❌ chat-widget MISSING from client/index.html"; exit 1; }
